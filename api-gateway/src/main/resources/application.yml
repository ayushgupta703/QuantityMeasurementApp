spring:
  codec:
    max-in-memory-size: 10MB

  cloud:
    loadbalancer:
      ribbon:
        enabled: false

    gateway:

      # ✅ GLOBAL FILTERS (IMPORTANT)
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Origin Access-Control-Allow-Credentials

      routes:

        # 🔐 AUTH SERVICE (REGISTER + LOGIN)
        - id: auth-service
          uri: https://quantitymeasurementapp-authservice-production.up.railway.app
          predicates:
            - Path=/auth/**
          filters:
            - name: Retry
              args:
                retries: 3

        # 🔐 OAUTH ROUTES
        - id: auth-oauth
          uri: https://quantitymeasurementapp-authservice-production.up.railway.app
          predicates:
            - Path=/oauth2/**, /login/**

        # ⚙️ MEASUREMENT SERVICE
        - id: measurement-service
          uri: https://quantitymeasurementapp-measurementservice-production.up.railway.app
          predicates:
            - Path=/api/**

